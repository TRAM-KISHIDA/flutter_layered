name: flutter_layered
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  dio:
  freezed_annotation: 
  hooks_riverpod:
  json_annotation: 
  retrofit: 
  riverpod_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  build_runner: 
  freezed:
  json_serializable: 
  retrofit_generator:
  riverpod_generator:

flutter:
  uses-material-design: true
